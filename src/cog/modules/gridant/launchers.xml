<project name="Launchers" default="create" basedir=".">
	<target name="create">
<!--
		<ant antfile="${main.buildfile}" target="launcher">
			<property name="launcher-name" value="graph-viewer"/>
			<property name="class-name" value="org.globus.ogce.gui.grapheditor.generic.GraphFrame"/>
		</ant>
-->
		<copy file="etc/gridant-setenv" todir="${dist.dir}/bin"/>
		<chmod perm="+x" file="${dist.dir}/bin/gridant-setenv"/>
	</target>
</project>
